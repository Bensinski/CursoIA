{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Calculator on Port 8080",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/server.js",
            "console": "integratedTerminal",
            "skipFiles": ["<node_internals>/**"]
        }
    ]
}